{
  "name": "${localEnv:WEB_WORKSPACE_SLUG}",
  "dockerComposeFile": "${localEnv:WEB_DOCKER_COMPOSE_PATH}",
  "service": "app",
  "workspaceFolder": "${localEnv:WORKSPACE_PATH}${localEnv:WEB_WORKSPACE_PATH}",
  "remoteUser": "${localEnv:WEB_WORKSPACE_USER}",
  "shutdownAction": "stopCompose"
}
